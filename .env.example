# Copy to .env — publishable key from Stripe Dashboard (pk_test_...)
VITE_STRIPE_PUBLISHABLE_KEY=
# Optional: if frontend is not proxied, set API base (default "" uses /api on same host)
# VITE_API_BASE=
