# Netlify: connect this repo, use defaults below or set in the UI
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Node 20 LTS is a safe default for Vite 5; Netlify can override
  NODE_VERSION = "20"
